git : warning: in the working copy of 'app/templates/complementos/secao_edit_conteudo_upload.html', LF will be 
replaced by CRLF the next time Git touches it
No linha:1 caractere:196
+ ... atividades; git diff e1a7c78 -- app/templates/complementos/secao_edit ...
+                 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (warning: in the... Git touches it:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
diff --git a/app/templates/complementos/secao_edit_conteudo_upload.html b/app/templates/complementos/secao_edit_conteudo_upload.html
index 3b2b5ad..0f3f137 100644
--- a/app/templates/complementos/secao_edit_conteudo_upload.html
+++ b/app/templates/complementos/secao_edit_conteudo_upload.html
@@ -70,17 +70,19 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
               <select
                 id="sel-sec-responsavel"
                 data-sec-tem-upload="{{ '1' if sec_tem_upload else '0' }}"
-                title="Responsável pela seção{% if sec_tem_upload %} (obrigatório quando há conteúdo inserido pelo autor){% endif %}"
-                {% if sec_tem_upload %}required{% endif %}
+                title="Responsável pela seção{%- if sec_tem_upload -%} (obrigatório quando há conteúdo inserido pelo autor){%- endif -%}"
+                {%- if sec_tem_upload %} required{%- endif %}
               >
                 {% if not sec_tem_upload %}
                 <option value="">—</option>
                 {% endif %}
                 {% for a in autores %}
+                {% set _resp_none = sec.responsavel_id == None %}
+                {% set _sel_autor = sec.responsavel_id == a.id or (_resp_none and user.role == 'autor' and a.id == user.id) %}
                 <option
                   value="{{ a.id }}"
                   {% if a.id != user.id and user.role == 'autor' %}disabled{% endif %}
-                  {% if sec.responsavel_id == a.id or (sec.responsavel_id is none and user.role == 'autor' and a.id == user.id) %}selected{% endif %}
+                  {% if _sel_autor %}selected{% endif %}
                 >
                   {{ a.nome }}
                 </option>
@@ -194,8 +196,9 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
       class="ss ss-sub active-tab{% if not blocos_escopo %} is-hidden{% endif %}"
       id="ss-blocos"
     >
-      <div class="ss-hdr">
+      <div class="ss-hdr" style="cursor:pointer" id="ss-blocos-hdr">
         <span class="sn">2.1</span><span class="st">Blocos extraídos (subsecções incluídas)</span>
+        <button type="button" id="ss-blocos-toggle" class="ss-toggle-btn" aria-expanded="true" title="Recolher/expandir" style="margin-left:auto;background:none;border:none;cursor:pointer;font-size:16px;color:var(--navy);padding:0 4px;">▾</button>
       </div>
       <div class="sb2 blocos-table-wrap">
         {% if blocos_escopo %}
@@ -236,6 +239,19 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
             Excluir em lote
           </button>
         </div>
+        <div class="blocos-filter-bar">
+          <input type="text" id="blocos-filter-indice" class="blocos-filter-input" placeholder="Filtrar Índice…" aria-label="Filtrar por índice da seção">
+          <input type="text" id="blocos-filter-titulo" class="blocos-filter-input" placeholder="Filtrar Título…" aria-label="Filtrar por título da seção">
+          <select id="blocos-filter-tipo" class="blocos-filter-input" aria-label="Filtrar por tipo">
+            <option value="">Todos os tipos</option>
+            <option value="texto">Texto</option>
+            <option value="lista">Lista</option>
+            <option value="figura">Figura</option>
+            <option value="tabela">Tabela</option>
+          </select>
+          <span class="blocos-pag-info" id="blocos-pag-info"></span>
+          <div class="blocos-pag-btns" id="blocos-pag-btns"></div>
+        </div>
         <table class="blocos-table">
           <thead>
             <tr>
@@ -522,6 +538,18 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
               class="edit-bloco-tabela-meta"
             ></span>
           </div>
+          <div class="blocos-filter-bar" id="edit-blocos-filter-bar">
+            <input type="text" id="edit-filter-sec" class="blocos-filter-input" placeholder="Filtrar Nº seção…" aria-label="Filtrar por número de seção">
+            <select id="edit-filter-tipo" class="blocos-filter-input" aria-label="Filtrar por tipo">
+              <option value="">Todos os tipos</option>
+              <option value="texto">Texto</option>
+              <option value="lista">Lista</option>
+              <option value="figura">Figura</option>
+              <option value="tabela">Tabela</option>
+            </select>
+            <span class="blocos-pag-info" id="edit-blocos-pag-info"></span>
+            <div class="blocos-pag-btns" id="edit-blocos-pag-btns"></div>
+          </div>
           <table class="bloco-table edit-bloco-tabela">
             <thead>
               <tr>
@@ -1013,6 +1041,150 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     </section>
   </aside>
 </div>
+<script>
+(function () {
+  /* ======= BLOCOS-TABLE: filtro + paginação + collapse ======= */
+  var PG = 20;
+
+  // Collapse da seção ss-blocos
+  var ssHdr = document.getElementById('ss-blocos-hdr');
+  var ssToggle = document.getElementById('ss-blocos-toggle');
+  var ssBlocos = document.getElementById('ss-blocos');
+  if (ssHdr && ssToggle && ssBlocos) {
+    var sbBody = ssBlocos.querySelector('.sb2');
+    ssHdr.addEventListener('click', function (ev) {
+      if (ev.target.closest('input, select, button') && ev.target !== ssToggle) return;
+      var open = ssToggle.getAttribute('aria-expanded') !== 'false';
+      ssToggle.setAttribute('aria-expanded', String(!open));
+      ssToggle.classList.toggle('collapsed', open);
+      if (sbBody) sbBody.style.display = open ? 'none' : '';
+    });
+  }
+
+  // Filtro + paginação da blocos-table
+  var tbl = document.querySelector('.blocos-table');
+  if (tbl) {
+    var allRows = Array.from(tbl.querySelectorAll('tbody tr'));
+    var filtIndice = document.getElementById('blocos-filter-indice');
+    var filtTitulo = document.getElementById('blocos-filter-titulo');
+    var filtTipo   = document.getElementById('blocos-filter-tipo');
+    var pagInfo    = document.getElementById('blocos-pag-info');
+    var pagBtns    = document.getElementById('blocos-pag-btns');
+    var curPage    = 1;
+
+    function getVisible() {
+      var vi = (filtIndice ? filtIndice.value.trim().toLowerCase() : '');
+      var vt = (filtTitulo ? filtTitulo.value.trim().toLowerCase() : '');
+      var tp = (filtTipo   ? filtTipo.value.trim().toLowerCase() : '');
+      return allRows.filter(function (row) {
+        var cells = row.querySelectorAll('td');
+        var indice = cells[1] ? cells[1].textContent.trim().toLowerCase() : '';
+        var titulo = cells[2] ? cells[2].textContent.trim().toLowerCase() : '';
+        var tipo   = cells[3] ? cells[3].textContent.trim().toLowerCase() : '';
+        return (!vi || indice.includes(vi))
+            && (!vt || titulo.includes(vt))
+            && (!tp || tipo.includes(tp));
+      });
+    }
+
+    function renderPage() {
+      var visible = getVisible();
+      var total = visible.length;
+      var pages = Math.max(1, Math.ceil(total / PG));
+      if (curPage > pages) curPage = 1;
+      allRows.forEach(function (r) { r.style.display = 'none'; });
+      var start = (curPage - 1) * PG;
+      visible.slice(start, start + PG).forEach(function (r) { r.style.display = ''; });
+      if (pagInfo) pagInfo.textContent = total + ' registro(s) · pág ' + curPage + '/' + pages;
+      if (pagBtns) {
+        pagBtns.innerHTML = '';
+        for (var p = 1; p <= pages; p++) {
+          var btn = document.createElement('button');
+          btn.type = 'button';
+          btn.className = 'blocos-pag-btn' + (p === curPage ? ' active' : '');
+          btn.textContent = p;
+          btn.dataset.p = p;
+          pagBtns.appendChild(btn);
+        }
+      }
+    }
+
+    [filtIndice, filtTitulo, filtTipo].forEach(function (el) {
+      if (el) el.addEventListener('input', function () { curPage = 1; renderPage(); });
+    });
+    if (pagBtns) pagBtns.addEventListener('click', function (ev) {
+      var btn = ev.target.closest('.blocos-pag-btn');
+      if (btn) { curPage = Number(btn.dataset.p); renderPage(); }
+    });
+
+    renderPage();
+  }
+
+  /* ======= EDIT-BLOCO-TABELA: filtro + paginação ======= */
+  (function () {
+    var editPG = 20;
+    var editPage = 1;
+    var editAllRows = [];
+
+    var filtSec  = document.getElementById('edit-filter-sec');
+    var filtTipo = document.getElementById('edit-filter-tipo');
+    var pagInfo  = document.getElementById('edit-blocos-pag-info');
+    var pagBtns  = document.getElementById('edit-blocos-pag-btns');
+    var tblBody  = document.getElementById('edit-bloco-tabela-body');
+    if (!tblBody) return;
+
+    function refresh() {
+      editAllRows = Array.from(tblBody.querySelectorAll('tr'));
+      renderEditPage();
+    }
+
+    function getEditVisible() {
+      var vs = filtSec  ? filtSec.value.trim().toLowerCase()  : '';
+      var vt = filtTipo ? filtTipo.value.trim().toLowerCase() : '';
+      return editAllRows.filter(function (row) {
+        var cells = row.querySelectorAll('td');
+        var sec  = cells[0] ? cells[0].textContent.trim().toLowerCase() : '';
+        var tipo = cells[1] ? cells[1].textContent.trim().toLowerCase() : '';
+        return (!vs || sec.includes(vs)) && (!vt || tipo.includes(vt));
+      });
+    }
+
+    function renderEditPage() {
+      var visible = getEditVisible();
+      var total = visible.length;
+      var pages = Math.max(1, Math.ceil(total / editPG));
+      if (editPage > pages) editPage = 1;
+      editAllRows.forEach(function (r) { r.style.display = 'none'; });
+      var start = (editPage - 1) * editPG;
+      visible.slice(start, start + editPG).forEach(function (r) { r.style.display = ''; });
+      if (pagInfo) pagInfo.textContent = total + ' registro(s) · pág ' + editPage + '/' + pages;
+      if (pagBtns) {
+        pagBtns.innerHTML = '';
+        for (var p = 1; p <= pages; p++) {
+          var btn = document.createElement('button');
+          btn.type = 'button';
+          btn.className = 'blocos-pag-btn' + (p === editPage ? ' active' : '');
+          btn.textContent = p;
+          btn.dataset.p = p;
+          pagBtns.appendChild(btn);
+        }
+      }
+    }
+
+    if (filtSec)  filtSec.addEventListener('input',  function () { editPage = 1; renderEditPage(); });
+    if (filtTipo) filtTipo.addEventListener('input',  function () { editPage = 1; renderEditPage(); });
+    if (pagBtns)  pagBtns.addEventListener('click',  function (ev) {
+      var btn = ev.target.closest('.blocos-pag-btn');
+      if (btn) { editPage = Number(btn.dataset.p); renderEditPage(); }
+    });
+
+    // Observar mutações no tblBody (ele é preenchido por JS após carregar blocos).
+    if (typeof MutationObserver !== 'undefined') {
+      new MutationObserver(refresh).observe(tblBody, { childList: true });
+    }
+  })();
+})();
+</script>
 <script src="/static/js/import_texto_tools.js"></script>
 <script>
   (function () {
@@ -1223,6 +1395,150 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     applyZoom();
   })();
 </script>
+<script>
+  /* Sincroniza a se\u00e7\u00e3o 4 (iframe de pr\u00e9-visualiza\u00e7\u00e3o) com a se\u00e7\u00e3o 2 (import-list):
+     - Ao rolar a import-list, a pr\u00e9-visualiza\u00e7\u00e3o acompanha a se\u00e7\u00e3o mais vis\u00edvel.
+     - Ao clicar em um card ou header de grupo, a pr\u00e9-visualiza\u00e7\u00e3o pula para a se\u00e7\u00e3o.
+     O iframe \u00e9 same-origin (/relatorios/{id}/preview), ent\u00e3o acessamos o documento diretamente. */
+  (function () {
+    const frame = document.getElementById("preview-frame");
+    const list = document.getElementById("import-list");
+    if (!frame || !list) return;
+
+    let rafPending = false;
+    let suppressUntil = 0;
+    let lastAnchor = "";
+
+    function getFrameDoc() {
+      try {
+        return (
+          frame.contentDocument ||
+          (frame.contentWindow && frame.contentWindow.document) ||
+          null
+        );
+      } catch (err) {
+        return null;
+      }
+    }
+
+    function anchorFromNumero(numero) {
+      const n = String(numero == null ? "" : numero).trim();
+      if (!n) return "";
+      return "sec-" + n.replace(/\./g, "-");
+    }
+
+    function numeroSecaoFromEl(el) {
+      if (!el) return "";
+      const section = el.closest(".import-secao");
+      if (!section) return "";
+      const numInput = section.querySelector(".import-secao-num");
+      const fromInput = numInput ? String(numInput.value || "").trim() : "";
+      if (fromInput) return fromInput;
+      const key = section.getAttribute("data-key") || "";
+      if (!key || key === "__atual__") return "";
+      return key;
+    }
+
+    function scrollFrameTo(anchorId, behavior) {
+      if (!anchorId) return;
+      const doc = getFrameDoc();
+      if (!doc) return;
+      const target = doc.getElementById(anchorId);
+      if (!target) return;
+      try {
+        target.scrollIntoView({
+          behavior: behavior || "smooth",
+          block: "start",
+          inline: "nearest",
+        });
+      } catch (err) {
+        target.scrollIntoView();
+      }
+    }
+
+    function secaoMaisVisivelNoImportList() {
+      const secs = list.querySelectorAll(".import-secao");
+      if (!secs.length) return "";
+      const listRect = list.getBoundingClientRect();
+      const refY = listRect.top + 12;
+      let melhor = null;
+      let melhorDist = Infinity;
+      secs.forEach((sec) => {
+        const hdr = sec.querySelector(".import-secao-hdr");
+        const ref = hdr || sec;
+        const r = ref.getBoundingClientRect();
+        if (r.bottom < listRect.top - 4) return;
+        if (r.top > listRect.bottom + 4) return;
+        const dist = Math.abs(r.top - refY);
+        if (dist < melhorDist) {
+          melhorDist = dist;
+          melhor = sec;
+        }
+      });
+      if (!melhor) return "";
+      const numInput = melhor.querySelector(".import-secao-num");
+      const fromInput = numInput ? String(numInput.value || "").trim() : "";
+      if (fromInput) return fromInput;
+      const key = melhor.getAttribute("data-key") || "";
+      if (!key || key === "__atual__") return "";
+      return key;
+    }
+
+    function syncFromScroll() {
+      if (Date.now() < suppressUntil) return;
+      const numero = secaoMaisVisivelNoImportList();
+      const anchor = anchorFromNumero(numero);
+      if (!anchor || anchor === lastAnchor) return;
+      lastAnchor = anchor;
+      scrollFrameTo(anchor, "smooth");
+    }
+
+    function onScroll() {
+      if (rafPending) return;
+      rafPending = true;
+      window.requestAnimationFrame(() => {
+        rafPending = false;
+        syncFromScroll();
+      });
+    }
+
+    list.addEventListener("scroll", onScroll, { passive: true });
+    window.addEventListener("scroll", onScroll, {
+      passive: true,
+      capture: true,
+    });
+
+    list.addEventListener(
+      "click",
+      (ev) => {
+        const t = ev.target;
+        if (!t) return;
+        const ignorar = t.closest(
+          "input, select, textarea, .import-secao-toggle, .import-secao-bulk-btn, .import-tx-tool, .import-raw-close, .import-raw, .seg-chip[data-seg-toggle]",
+        );
+        if (ignorar) return;
+        const card = t.closest(".import-card");
+        const hdr = t.closest(".import-secao-hdr");
+        const ref = card || hdr;
+        if (!ref) return;
+        const numero = numeroSecaoFromEl(ref);
+        const anchor = anchorFromNumero(numero);
+        if (!anchor) return;
+        suppressUntil = Date.now() + 600;
+        lastAnchor = anchor;
+        scrollFrameTo(anchor, "smooth");
+      },
+      true,
+    );
+
+    frame.addEventListener("load", () => {
+      lastAnchor = "";
+      setTimeout(syncFromScroll, 120);
+    });
+
+    setTimeout(syncFromScroll, 300);
+  })();
+</script>
 <script
   data-rel-id="{{ rel.id|tojson|forceescape }}"
   data-sec-numero="{{ sec.numero|tojson|forceescape }}"
@@ -2518,18 +2834,75 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
       );
     }
 
-    function confirmar() {
+    async function confirmar() {
       if (!_blocoIdEmEdicao) {
         setStatus("Carregue um bloco antes de confirmar.", "warn");
         return;
       }
-      formAdd.submit();
+      const pe = parseEditTarget();
+      if (!pe) {
+        setStatus("Não foi possível determinar o alvo de edição.", "danger");
+        return;
+      }
+      // Flush do editor para o rascunho local antes de enviar ao servidor
+      salvar();
+      const draft = lerRascunho(_blocoIdEmEdicao);
+      const titulo = draft ? draft.titulo : (inpTitulo.value || "");
+      const conteudo = draft ? draft.conteudo : (taConteudo.value || "");
+      const fd = new FormData();
+      fd.append("titulo", titulo);
+      fd.append("conteudo", conteudo);
+      // Campos extras que o servidor pode precisar (legenda/fonte/figura_id ficam
+      // em branco quando não disponíveis via rascunho — o servidor mantém o valor original)
+      fd.append("legenda", "");
+      fd.append("fonte", "");
+      fd.append("figura_id", "");
+      const url =
+        "/relatorios/" +
+        pe.relId +
+        "/secoes/" +
+        pe.secaoId +
+        "/blocos/" +
+        pe.blocoId +
+        "/editar";
+      setStatus("Enviando bloco #" + pe.blocoId + " ao servidor…", "working");
+      try {
+        const r = await fetch(url, {
+          method: "POST",
+          body: fd,
+          credentials: "same-origin",
+        });
+        if (!r.ok) {
+          setStatus(
+            "Falha ao confirmar bloco #" + pe.blocoId + " (HTTP " + r.status + ").",
+            "danger",
+          );
+          return;
+        }
+        // Remove o rascunho local após sucesso no servidor
+        window.localStorage.removeItem(localKey(_blocoIdEmEdicao));
+        setStatus(
+          "Bloco #" + pe.blocoId + " enviado ao servidor com sucesso.",
+          "ok",
+        );
+        _blocoIdEmEdicao = null;
+        btnSalvar.disabled = true;
+        btnConfirmar.disabled = true;
+      } catch (e) {
+        setStatus("Falha ao confirmar: " + e.message, "danger");
+      }
     }
 
     if (formAdd) {
-      formAdd.addEventListener("submit", () => {
-        const m = formAdd.action.match(/\/blocos\/(\d+)\/editar(?:\?|$|\/|#)/);
-        if (m) window.localStorage.removeItem(localKey(m[1]));
+      formAdd.addEventListener("submit", (e) => {
+        // Em modo edição, intercepta o submit: salva apenas localmente (rascunho).
+        // O envio ao servidor fica a cargo de btn-edit-confirmar.
+        const isEdit = formAdd.action.indexOf("/editar") !== -1;
+        if (isEdit) {
+          e.preventDefault();
+          salvar();
+          return;
+        }
       });
     }
 
@@ -2783,34 +3156,102 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
       return "usar seção";
     }
 
+    /** Divide o conteúdo bruto do bloco em pedaços que correspondam aos subtipos detectados.
+        Para parágrafo/lista o backend agrega várias linhas num único segmento (count>1),
+        mas o JS reconstrói a visualização individual a partir de b.conteudo. */
+    function segmentosDetalhados(subtipos, conteudoBruto) {
+      const linhas = String(conteudoBruto || "").split(/\r?\n/);
+      const out = [];
+      let li = 0;
+      const totalLinhas = linhas.length;
+      const avancarVazias = () => {
+        while (li < totalLinhas && !linhas[li].trim()) li++;
+      };
+      const consumirLinha = () => {
+        if (li < totalLinhas) {
+          const v = linhas[li];
+          li++;
+          return v;
+        }
+        return "";
+      };
+      (subtipos || []).forEach((s) => {
+        const kind = s.kind || "paragrafo";
+        const count = Math.max(1, Number(s.count || 1));
+        const previews = [];
+        avancarVazias();
+        if (kind === "titulo") {
+          const l = consumirLinha();
+          previews.push(l.replace(/^#\s+/, "").trim());
+        } else if (kind === "subtitulo") {
+          const l = consumirLinha();
+          previews.push(l.replace(/^##\s+/, "").trim());
+        } else if (kind === "lista") {
+          const itens = [];
+          for (let k = 0; k < count && li < totalLinhas; k++) {
+            avancarVazias();
+            const l = consumirLinha();
+            if (l.trim()) itens.push(l.trim());
+          }
+          itens.forEach((t) => previews.push(t));
+        } else {
+          // parágrafo: cada parágrafo é uma linha não vazia separada por linhas em branco
+          for (let k = 0; k < count && li < totalLinhas; k++) {
+            avancarVazias();
+            const buf = [];
+            while (li < totalLinhas && linhas[li].trim()) {
+              buf.push(linhas[li].trim());
+              li++;
+            }
+            if (buf.length) previews.push(buf.join(" "));
+          }
+        }
+        if (!previews.length && s.preview) previews.push(String(s.preview));
+        out.push({ kind, count, previews });
+      });
+      return out;
+    }
+
     // Constrói os HTML para cada subtipo (chips de TEXTO/LISTA).
-    function renderSubtipos(subtipos) {
+    // Layout em 2 linhas por item: chip (clicável, alterna para o import-raw) + lista de previews individuais.
+    function renderSubtipos(subtipos, conteudoBruto) {
       if (!Array.isArray(subtipos) || !subtipos.length) {
         return '<div class="import-segs-empty">sem segmentos detectados</div>';
       }
-      const items = subtipos
+      const detalhados = segmentosDetalhados(subtipos, conteudoBruto);
+      const items = detalhados
         .map((s) => {
-          const kind = s.kind || "paragrafo";
+          const kind = s.kind;
           const label = SUBTIPO_LABEL[kind] || kind;
-          const count = Number(s.count || 0);
+          const count = s.count;
           const suffix =
             kind === "lista" && count > 1
               ? " (" + count + " itens)"
               : kind === "paragrafo" && count > 1
                 ? " (" + count + ")"
                 : "";
+          const previewsHtml = (s.previews || [])
+            .map(
+              (p) =>
+                '<li class="seg-preview-item">' + esc(p || "") + "</li>",
+            )
+            .join("");
           return (
             '<li class="seg-item" data-kind="' +
             esc(kind) +
             '">' +
-            '<span class="seg-chip seg-chip-' +
+            '<div class="seg-row seg-row-chip">' +
+            '<button type="button" class="seg-chip seg-chip-' +
             esc(kind) +
-            '">' +
+            '" data-seg-toggle="1" title="Clique para editar o texto bruto">' +
             esc(label + suffix) +
-            "</span>" +
-            '<span class="seg-preview">' +
-            esc(s.preview || "") +
-            "</span>" +
+            "</button>" +
+            "</div>" +
+            '<div class="seg-row seg-row-preview">' +
+            '<ul class="seg-preview-list">' +
+            previewsHtml +
+            "</ul>" +
+            "</div>" +
             "</li>"
           );
         })
@@ -2857,7 +3298,7 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
       );
     }
 
-    function tipoSelect(i, tipoAtual) {
+    function tipoSelectBadge(i, tipoAtual) {
       const opts = ["texto", "lista", "figura", "tabela"]
         .map((t) => {
           return (
@@ -2876,9 +3317,11 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
         i +
         '" name="import-tipo-sel-' +
         i +
-        '" class="import-tipo-sel" data-i="' +
+        '" class="import-tipo-badge import-tipo-sel" data-tipo="' +
+        esc(tipoAtual) +
+        '" data-i="' +
         i +
-        '" data-field="tipo" title="Mudar tipo do bloco">' +
+        '" data-field="tipo" title="Tipo identificado pelo sistema — clique para alterar">' +
         opts +
         "</select>"
       );
@@ -2890,8 +3333,15 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
       const confPct = Math.round(conf * 100);
       const confCls = confidenceClass(conf);
       const checked = b.selecionado !== false ? "checked" : "";
+      const motivo = b.motivo || "";
+      const confTip =
+        "Nível de confiança: estimativa (0–100%) de que o sistema classificou corretamente o tipo deste bloco com base na análise do arquivo. " +
+        "Faixas: ≥85% (alto) · 70–84% (médio) · <70% (baixo — revise antes de importar)." +
+        (motivo ? "\nMotivo: " + motivo : "");
       return (
-        '<header class="import-card-hdr">' +
+        '<header class="import-card-hdr" data-tipo="' +
+        esc(tipo) +
+        '">' +
         '<input type="checkbox" id="import-card-check-' +
         i +
         '" name="import-card-check-' +
@@ -2901,23 +3351,22 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
         '" data-field="selecionado" ' +
         checked +
         ">" +
-        '<span class="import-tipo-badge" data-tipo="' +
-        esc(tipo) +
-        '">' +
-        esc(TIPO_LABEL[tipo] || tipo.toUpperCase()) +
-        "</span>" +
+        tipoSelectBadge(i, tipo) +
+        '<span class="import-conf-wrap">' +
+        '<span class="import-conf-lbl" tabindex="0" data-conf-tip="' +
+        esc(confTip) +
+        '" title="' +
+        esc(confTip) +
+        '">Nível de confiança</span>' +
         '<span class="import-conf import-conf-' +
         confCls +
+        '" tabindex="0" data-conf-tip="' +
+        esc(confTip) +
         '" title="' +
-        esc(b.motivo || "") +
+        esc(confTip) +
         '">' +
         confPct +
         "%</span>" +
-        tipoSelect(i, tipo) +
-        '<span class="import-card-motivo" title="' +
-        esc(b.motivo || "") +
-        '">' +
-        esc(b.motivo || "") +
         "</span>" +
         "</header>"
       );
@@ -2930,9 +3379,10 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
         '">' +
         renderCardHeader(b, i) +
         '<div class="import-card-body">' +
-        renderSubtipos(b.subtipos) +
-        '<details class="import-raw">' +
-        "<summary>editar texto bruto</summary>" +
+        '<div class="import-segs-view">' +
+        renderSubtipos(b.subtipos, b.conteudo) +
+        "</div>" +
+        '<div class="import-raw is-hidden" data-raw-mode="1">' +
         '<div class="import-raw-tb" role="toolbar" aria-label="Ferramentas de listas (texto)">' +
         '<span class="import-raw-tb-hint">Listas e níveis (só o texto, não a estrutura de seções):</span>' +
         '<button type="button" class="secondary-mini import-tx-tool" data-import-tx-tool="bullet" title="Itens com marcador">• Marcadores</button>' +
@@ -2941,6 +3391,7 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
         '<button type="button" class="secondary-mini import-tx-tool" data-import-tx-tool="oli" title="i. ii. iii. (romano)">i. ii. iii.</button>' +
         '<button type="button" class="secondary-mini import-tx-tool" data-import-tx-tool="indent" title="Aumentar nível (2 espaços)">Nível +</button>' +
         '<button type="button" class="secondary-mini import-tx-tool" data-import-tx-tool="outdent" title="Diminuir nível">Nível −</button>' +
+        '<button type="button" class="secondary-mini import-raw-close" data-raw-close="1" title="Salvar alterações e voltar aos segmentos">✓ Salvar e fechar</button>' +
         "</div>" +
         '<textarea id="import-raw-ta-' +
         i +
@@ -2951,7 +3402,7 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
         '" data-field="conteudo" rows="6">' +
         esc(b.conteudo || "") +
         "</textarea>" +
-        "</details>" +
+        "</div>" +
         "</div>" +
         "</article>"
       );
@@ -2972,7 +3423,8 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     function renderFiguraCard(b, i, indiceSug) {
       const hasImg = b.image_b64 && b.image_mime;
       const idxRow = indiceSug
-        ? '<label class="mini-label" for="imp-fig-' +
+        ? '<div class="import-meta-row">' +
+          '<label class="mini-label" for="imp-fig-' +
           i +
           '-idx">Índice</label>' +
           '<input id="imp-fig-' +
@@ -2980,7 +3432,7 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
           '-idx" type="text" class="import-indice-readonly" readonly tabindex="-1" ' +
           ' value="' +
           esc(indiceSug) +
-          '" title="Sugestão: numeração por capítulo (primeiro nível do índice da seção) e ordem no lote, somando o que já existe nesta seção.">'
+          '" title="Sugestão: numeração por capítulo (primeiro nível do índice da seção) e ordem no lote, somando o que já existe nesta seção."></div>'
         : "";
       const mimeSub = esc(
         String(b.image_mime || "").split("/")[1] || "img",
@@ -3008,22 +3460,22 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
         i +
         '">' +
         renderCardHeader(b, i) +
-        '<div class="import-card-body import-card-2col">' +
-        '<div class="import-fig-thumb">' +
-        thumb +
-        "</div>" +
+        '<div class="import-card-body import-card-2col import-card-2col-reverse">' +
         '<div class="import-meta-fields">' +
         idxRow +
+        '<div class="import-meta-row">' +
         '<label class="mini-label" for="imp-fig-' +
         i +
         '-leg">Legenda</label>' +
-        '<input id="imp-fig-' +
+        '<textarea id="imp-fig-' +
         i +
-        '-leg" type="text" data-i="' +
+        '-leg" rows="1" class="import-meta-textarea auto-grow" data-i="' +
         i +
-        '" data-field="legenda" value="' +
+        '" data-field="legenda" placeholder="ex.: Vista aérea da obra">' +
         esc(b.legenda || "") +
-        '" placeholder="ex.: Vista aérea da obra">' +
+        "</textarea>" +
+        "</div>" +
+        '<div class="import-meta-row">' +
         '<label class="mini-label" for="imp-fig-' +
         i +
         '-fonte">Fonte</label>' +
@@ -3036,13 +3488,18 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
         '" placeholder="ex.: Concremat 2025">' +
         "</div>" +
         "</div>" +
+        '<div class="import-fig-thumb">' +
+        thumb +
+        "</div>" +
+        "</div>" +
         "</article>"
       );
     }
 
     function renderTabelaCard(b, i, indiceSug) {
       const idxRow = indiceSug
-        ? '<label class="mini-label" for="imp-tab-' +
+        ? '<div class="import-meta-row">' +
+          '<label class="mini-label" for="imp-tab-' +
           i +
           '-idx">Índice</label>' +
           '<input id="imp-tab-' +
@@ -3050,29 +3507,30 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
           '-idx" type="text" class="import-indice-readonly" readonly tabindex="-1" ' +
           ' value="' +
           esc(indiceSug) +
-          '" title="Sugestão: numeração por capítulo (primeiro nível do índice da seção) e ordem no lote, somando o que já existe nesta seção.">'
+          '" title="Sugestão: numeração por capítulo (primeiro nível do índice da seção) e ordem no lote, somando o que já existe nesta seção."></div>'
         : "";
       return (
         '<article class="import-card import-card-tabela" data-i="' +
         i +
         '">' +
         renderCardHeader(b, i) +
-        '<div class="import-card-body import-card-2col">' +
-        '<div class="import-tab-prev">' +
-        renderTabelaPreview(b.tabela_preview) +
-        "</div>" +
+        '<div class="import-card-body">' +
+        '<div class="import-card-2col import-card-2col-reverse">' +
         '<div class="import-meta-fields">' +
         idxRow +
+        '<div class="import-meta-row">' +
         '<label class="mini-label" for="imp-tab-' +
         i +
         '-leg">Legenda</label>' +
-        '<input id="imp-tab-' +
+        '<textarea id="imp-tab-' +
         i +
-        '-leg" type="text" data-i="' +
+        '-leg" rows="1" class="import-meta-textarea auto-grow" data-i="' +
         i +
-        '" data-field="legenda" value="' +
+        '" data-field="legenda" placeholder="ex.: Quantitativo de serviços">' +
         esc(b.legenda || "") +
-        '" placeholder="ex.: Quantitativo de serviços">' +
+        "</textarea>" +
+        "</div>" +
+        '<div class="import-meta-row">' +
         '<label class="mini-label" for="imp-tab-' +
         i +
         '-fonte">Fonte</label>' +
@@ -3083,7 +3541,13 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
         '" data-field="fonte" value="' +
         esc(b.fonte || "") +
         '" placeholder="ex.: Diário de obra">' +
-        '<details class="import-raw">' +
+        "</div>" +
+        "</div>" +
+        '<div class="import-tab-prev">' +
+        renderTabelaPreview(b.tabela_preview) +
+        "</div>" +
+        "</div>" +
+        '<details class="import-raw import-raw-tabela-full">' +
         "<summary>editar dados brutos da tabela</summary>" +
         '<textarea id="import-raw-ta-' +
         i +
@@ -3091,12 +3555,11 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
         i +
         '" class="import-raw-ta" data-i="' +
         i +
-        '" data-field="conteudo" rows="6">' +
+        '" data-field="conteudo" rows="8">' +
         esc(b.conteudo || "") +
         "</textarea>" +
         "</details>" +
         "</div>" +
-        "</div>" +
         "</article>"
       );
     }
@@ -3247,6 +3710,11 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
       window.requestAnimationFrame(() => {
         list.innerHTML = html;
         updateSummary();
+        // Ajusta a altura inicial das textareas com auto-grow (legenda etc.).
+        list.querySelectorAll(".auto-grow").forEach((el) => {
+          el.style.height = "auto";
+          el.style.height = el.scrollHeight + "px";
+        });
       });
     }
 
@@ -3320,9 +3788,13 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
       });
     }
 
-    // ---- Handlers (delegação no list) ----
+    // ---- Handlers (delega\u00e7\u00e3o no list) ----
     list.addEventListener("input", (ev) => {
       const el = ev.target;
+      if (el && el.classList && el.classList.contains("auto-grow")) {
+        el.style.height = "auto";
+        el.style.height = el.scrollHeight + "px";
+      }
       if (el.dataset.field) {
         const idx = Number(el.dataset.i);
         if (!Number.isFinite(idx) || !importBlocks[idx]) return;
@@ -3397,6 +3869,53 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
         ev.preventDefault();
         return;
       }
+      const segChip = ev.target.closest(".seg-chip[data-seg-toggle]");
+      if (segChip) {
+        const card = segChip.closest(".import-card");
+        if (card) {
+          const raw = card.querySelector(".import-raw[data-raw-mode]");
+          if (raw) {
+            raw.classList.remove("is-hidden");
+            const ta = raw.querySelector(".import-raw-ta");
+            if (ta) {
+              try { ta.focus({ preventScroll: true }); } catch (e) { ta.focus(); }
+            }
+          }
+        }
+        ev.preventDefault();
+        return;
+      }
+      const rawClose = ev.target.closest(".import-raw-close[data-raw-close]");
+      if (rawClose) {
+        const card = rawClose.closest(".import-card");
+        if (card) {
+          const raw = card.querySelector(".import-raw[data-raw-mode]");
+          if (raw) {
+            // Garante que o valor digitado foi persistido em importBlocks (mesmo sem blur).
+            const ta = raw.querySelector(".import-raw-ta");
+            if (ta) {
+              const idx = Number(ta.dataset.i);
+              if (Number.isFinite(idx) && importBlocks[idx]) {
+                importBlocks[idx].conteudo = ta.value;
+              }
+            }
+            raw.classList.add("is-hidden");
+            // Re-renderiza só este card para atualizar os previews a partir do novo conteudo.
+            const i = Number(card.getAttribute("data-i"));
+            if (Number.isFinite(i) && importBlocks[i]) {
+              const segsView = card.querySelector(".import-segs-view");
+              if (segsView) {
+                segsView.innerHTML = renderSubtipos(
+                  importBlocks[i].subtipos,
+                  importBlocks[i].conteudo,
+                );
+              }
+            }
+          }
+        }
+        ev.preventDefault();
+        return;
+      }
       const toggle = ev.target.closest(".import-secao-toggle");
       if (toggle) {
         const sec = toggle.closest(".import-secao");
@@ -4035,9 +4554,17 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
 
   /* Cards de bloco */
   .import-card {
-    border: 1px solid var(--border);
-    border-radius: 4px;
+    border: 1px solid #b3c2d4;
+    border-radius: 5px;
     background: #fff;
+    box-shadow: 0 1px 2px rgba(19, 38, 64, 0.08);
+    min-height: 150px;
+    display: flex;
+    flex-direction: column;
+  }
+  .import-card:hover {
+    border-color: #7f96b3;
+    box-shadow: 0 2px 4px rgba(19, 38, 64, 0.12);
   }
   .import-card-hdr {
     display: flex;
@@ -4046,13 +4573,25 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     padding: 6px 10px;
     background: #f0f4fa;
     border-bottom: 1px solid var(--border);
-    flex-wrap: wrap;
+    flex-wrap: nowrap;
+    white-space: nowrap;
+    overflow: hidden;
+  }
+  .import-card-hdr[data-tipo="figura"] {
+    background: #d6ecf5;
+  }
+  .import-card-hdr[data-tipo="tabela"] {
+    background: #e0d7f0;
+  }
+  .import-card-hdr[data-tipo="lista"] {
+    background: #d7ecde;
   }
   .import-card-check {
     width: 14px;
     height: 14px;
     margin: 0;
     cursor: pointer;
+    flex: 0 0 auto;
   }
   .import-tipo-badge {
     font-size: 10px;
@@ -4063,14 +4602,63 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     background: var(--navy);
     color: #fff;
   }
-  .import-tipo-badge[data-tipo="figura"] {
-    background: #0e7da5;
+  /* Quando .import-tipo-badge \u00e9 um <select> (fus\u00e3o badge + seletor de tipo).
+     Cor neutra para preservar legibilidade em todos os tipos de cabe\u00e7alho. */
+  select.import-tipo-badge {
+    appearance: none;
+    -webkit-appearance: none;
+    -moz-appearance: none;
+    border: 1px solid #c9d4e0;
+    background-color: #f5f7fb;
+    color: var(--text);
+    font-family: inherit;
+    font-weight: 700;
+    font-size: 10.5px;
+    text-transform: uppercase;
+    letter-spacing: 0.05em;
+    padding: 2px 20px 2px 8px;
+    border-radius: 3px;
+    width: auto;
+    min-width: 0;
+    cursor: pointer;
+    line-height: 1.4;
+    background-image: linear-gradient(45deg, transparent 50%, #667 50%),
+      linear-gradient(135deg, #667 50%, transparent 50%);
+    background-position:
+      calc(100% - 10px) 55%,
+      calc(100% - 6px) 55%;
+    background-size:
+      4px 4px,
+      4px 4px;
+    background-repeat: no-repeat;
+    flex: 0 0 auto;
   }
-  .import-tipo-badge[data-tipo="tabela"] {
-    background: #5a3aa0;
+  select.import-tipo-badge option {
+    background: #fff;
+    color: var(--text);
+    font-weight: 600;
+    text-transform: none;
+    letter-spacing: normal;
   }
-  .import-tipo-badge[data-tipo="lista"] {
-    background: #1f7a3a;
+  select.import-tipo-badge:focus {
+    outline: 2px solid var(--blue);
+    outline-offset: 1px;
+  }
+  .import-conf-wrap {
+    display: inline-flex;
+    align-items: center;
+    gap: 6px;
+    margin-left: auto;
+    flex: 0 0 auto;
+  }
+  .import-conf-lbl {
+    font-size: 10.5px;
+    color: var(--muted);
+    text-decoration: underline dotted;
+    text-underline-offset: 2px;
+    cursor: help;
+    white-space: nowrap;
+    font-weight: 600;
   }
   .import-conf {
     font-size: 11px;
@@ -4093,48 +4681,38 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     background: #ffe1de;
     color: #a32219;
   }
-  .import-tipo-sel {
-    padding: 3px 6px;
-    font-size: 11px;
-    border: 1px solid var(--border);
-    border-radius: 3px;
-    background: #fff;
-    color: var(--text);
-    font-family: inherit;
-  }
-  .import-card-motivo {
-    font-size: 11px;
-    color: var(--muted);
-    flex: 1;
-    min-width: 120px;
-    overflow: hidden;
-    text-overflow: ellipsis;
-    white-space: nowrap;
-  }
 
   .import-card-body {
     padding: 10px 12px;
+    flex: 1 1 auto;
+    display: flex;
+    flex-direction: column;
   }
   .import-card-2col {
     display: grid;
-    grid-template-columns: 160px 1fr;
+    grid-template-columns: 220px 1fr;
     gap: 14px;
-    align-items: start;
+    align-items: stretch;
+    flex: 1 1 auto;
+  }
+  .import-card-2col-reverse {
+    grid-template-columns: 1fr 260px;
   }
   @media (max-width: 720px) {
-    .import-card-2col {
+    .import-card-2col,
+    .import-card-2col-reverse {
       grid-template-columns: 1fr;
     }
   }
 
-  /* Segmentos do TEXTO (chips + preview) */
+  /* Segmentos do TEXTO (chips + lista de previews). */
   .import-segs {
     list-style: none;
     margin: 0;
     padding: 0;
     display: flex;
     flex-direction: column;
-    gap: 5px;
+    gap: 6px;
   }
   .import-segs-empty {
     font-size: 11.5px;
@@ -4142,16 +4720,22 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     font-style: italic;
   }
   .seg-item {
-    display: grid;
-    grid-template-columns: 110px 1fr;
-    gap: 10px;
-    align-items: start;
-    padding: 3px 0;
+    display: flex;
+    flex-direction: column;
+    gap: 3px;
+    padding: 4px 0;
     border-bottom: 1px dotted #e3e9f0;
   }
   .seg-item:last-child {
     border-bottom: 0;
   }
+  .seg-row-chip {
+    display: flex;
+    align-items: center;
+  }
+  .seg-row-preview {
+    padding-left: 2px;
+  }
   .seg-chip {
     font-size: 10.5px;
     font-weight: 700;
@@ -4159,6 +4743,21 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     border-radius: 3px;
     text-align: center;
     letter-spacing: 0.04em;
+    display: inline-block;
+  }
+  button.seg-chip {
+    border: 1px solid transparent;
+    font-family: inherit;
+    cursor: pointer;
+    line-height: 1.4;
+  }
+  button.seg-chip:hover {
+    filter: brightness(0.96);
+    box-shadow: 0 0 0 1px rgba(0, 0, 0, 0.12);
+  }
+  button.seg-chip:focus {
+    outline: 2px solid var(--blue);
+    outline-offset: 1px;
   }
   .seg-chip-paragrafo {
     background: #e3eef7;
@@ -4176,6 +4775,18 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     background: #f7e3c5;
     color: #8a5b00;
   }
+  .seg-preview-list {
+    margin: 0;
+    padding-left: 18px;
+    list-style: disc outside;
+    font-size: 11.5px;
+    color: var(--text);
+    line-height: 1.45;
+  }
+  .seg-preview-item {
+    word-break: break-word;
+    margin: 1px 0;
+  }
   .seg-preview {
     font-size: 11.5px;
     color: var(--text);
@@ -4186,6 +4797,10 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
   .import-raw {
     margin-top: 8px;
   }
+  .import-raw.is-hidden,
+  .import-segs-view.is-hidden {
+    display: none !important;
+  }
   .import-raw > summary {
     cursor: pointer;
     font-size: 11px;
@@ -4199,6 +4814,29 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
   .import-raw[open] > summary {
     margin-bottom: 6px;
   }
+  .import-raw-close {
+    margin-left: auto;
+    font-size: 10.5px;
+    padding: 3px 10px;
+    background: #dff3e4;
+    color: #1f7a3a;
+    border: 1px solid #b7deb9;
+    font-weight: 600;
+    border-radius: 3px;
+    cursor: pointer;
+  }
+  .import-raw-close:hover {
+    background: #cbe9cf;
+  }
+  .import-raw-tabela-full {
+    width: 100%;
+    margin-top: 10px;
+    box-sizing: border-box;
+  }
+  .import-raw-tabela-full .import-raw-ta {
+    width: 100%;
+    box-sizing: border-box;
+  }
   .import-raw-tb {
     display: flex;
     flex-wrap: wrap;
@@ -4228,22 +4866,24 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     resize: vertical;
   }
 
-  /* Figura: thumbnail (sempre data URL na revisão quando o analisador envia image_b64) */
+  /* Figura: thumbnail (sempre data URL na revis\u00e3o quando o analisador envia image_b64) */
   .import-fig-thumb {
     border: 1px solid var(--border);
     border-radius: 3px;
-    padding: 4px;
+    padding: 6px;
     background: #fafbfd;
     display: flex;
     flex-direction: column;
     align-items: center;
     justify-content: center;
-    gap: 4px;
-    min-height: 120px;
+    gap: 6px;
+    min-height: 200px;
+    height: 100%;
+    box-sizing: border-box;
   }
   .import-fig-thumb img {
     max-width: 100%;
-    max-height: 140px;
+    max-height: 260px;
     width: auto;
     height: auto;
     object-fit: contain;
@@ -4265,10 +4905,13 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
   .import-tab-prev {
     border: 1px solid var(--border);
     border-radius: 3px;
-    padding: 4px;
+    padding: 6px;
     background: #fafbfd;
     overflow: auto;
-    max-height: 180px;
+    max-height: 280px;
+    min-height: 200px;
+    height: 100%;
+    box-sizing: border-box;
   }
   .import-mini-tbl {
     border-collapse: collapse;
@@ -4303,19 +4946,51 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     text-align: center;
   }
 
-  /* Campos de meta (legenda/fonte) à direita do thumb/mini-tbl */
+  /* Campos de meta (legenda/fonte) \u00e0 direita do thumb/mini-tbl */
   .import-meta-fields {
     display: flex;
     flex-direction: column;
-    gap: 4px;
+    gap: 0;
+    min-width: 0;
   }
-  .import-meta-fields .mini-label {
+  .import-meta-row {
+    display: flex;
+    align-items: center;
+    gap: 8px;
+    margin-bottom: 6px;
+    min-width: 0;
+  }
+  .import-meta-row:last-child {
+    margin-bottom: 0;
+  }
+  .import-meta-fields .mini-label,
+  .import-meta-row .mini-label {
     font-size: 10.5px;
     color: var(--muted);
     margin: 0;
     font-weight: 600;
     text-transform: uppercase;
     letter-spacing: 0.04em;
+    flex: 0 0 72px;
+    text-align: right;
+  }
+  .import-meta-row input[type="text"],
+  .import-meta-row textarea {
+    flex: 1 1 auto;
+    min-width: 180px;
+    max-width: 100%;
+    padding: 6px 8px;
+    font-size: 12px;
+    border: 1px solid var(--border);
+    border-radius: 3px;
+    font-family: inherit;
+    box-sizing: border-box;
+  }
+  .import-meta-textarea.auto-grow {
+    resize: none;
+    overflow: hidden;
+    line-height: 1.4;
+    min-height: 30px;
   }
   .import-meta-fields input[type="text"] {
     width: 100%;
@@ -4961,5 +5636,40 @@ sec.numero }} {{ sec.titulo }} — {{ rel.codigo }}{% endblock %} {% block heade
     background: var(--navy);
     color: #fff;
   }
+  /* ---- Filtros, paginação e toggle de seção ---- */
+  .ss-toggle-btn { transition: transform .2s; }
+  .ss-toggle-btn.collapsed { transform: rotate(-90deg); }
+  .blocos-filter-bar {
+    display: flex;
+    flex-wrap: wrap;
+    align-items: center;
+    gap: 8px;
+    padding: 8px 0 6px;
+  }
+  .blocos-filter-input {
+    padding: 4px 8px;
+    font-size: 11.5px;
+    border: 1px solid var(--border);
+    border-radius: 3px;
+    font-family: inherit;
+    background: #fff;
+    color: var(--text);
+    min-width: 120px;
+  }
+  .blocos-filter-input:focus { outline: 2px solid var(--blue); outline-offset: 1px; }
+  .blocos-pag-info { font-size: 11px; color: var(--muted); margin-left: auto; }
+  .blocos-pag-btns { display: flex; gap: 4px; }
+  .blocos-pag-btn {
+    padding: 3px 9px;
+    font-size: 11px;
+    border: 1px solid var(--border);
+    border-radius: 3px;
+    background: #fff;
+    cursor: pointer;
+    font-family: inherit;
+    color: var(--text);
+  }
+  .blocos-pag-btn.active { background: var(--navy); color: #fff; border-color: var(--navy); }
+  .blocos-pag-btn:hover:not(.active) { background: #f0f4fa; }
 </style>
 {% endblock %}
